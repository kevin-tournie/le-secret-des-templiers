{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "migrate": "bun run server/database/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.2",
    "nuxt": "^3.12.3",
    "vue": "latest"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "drizzle-kit": "^0.22.8"
  }
}